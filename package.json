{
  "name": "review-queue",
  "version": "0.1.0",
  "description": "Add reviewable PRs to the Review Queue",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "preversion": "npm test",
    "version": "npx @vercel/ncc build index.js --license licenses.txt && git add -A dist",
    "postversion": "git push && git push --tags"
  },
  "author": "max.beizer@gmail.com",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.3.0",
    "@actions/github": "^5.0.0",
    "@octokit/action": "^3.10.1"
  },
  "devDependencies": {
    "jest": "^27.0.6",
    "jest-cli": "^27.0.6"
  }
}
